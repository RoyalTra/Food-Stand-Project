<%= form_with(model: food, local: true) do |form| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :typesOfFood %>
    <%= form.text_field :typesOfFood, id: :food_typesOfFood %>
  </div>

  <div class="field">
    <%= form.label :prices %>
    <%= form.text_field :prices, id: :food_prices %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
