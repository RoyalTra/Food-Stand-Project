<%= form_with(model: checkout, local: true) do |form| %>
  <% if checkout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(checkout.errors.count, "error") %> prohibited this checkout from being saved:</h2>

      <ul>
      <% checkout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :checkout_id %>
    <%= form.text_field :checkout_id, id: :checkout_checkout_id %>
  </div>

  <div class="field">
    <%= form.label :number, %>
    <%= form.text_field :number,, id: :checkout_number, %>
  </div>

  <div class="field">
    <%= form.label :totalCost %>
    <%= form.text_field :totalCost, id: :checkout_totalCost %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :checkout_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
