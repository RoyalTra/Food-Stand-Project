<%= form_with(model: entree, local: true) do |form| %>
  <% if entree.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entree.errors.count, "error") %> prohibited this entree from being saved:</h2>

      <ul>
      <% entree.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :typesOfEntrees %>
    <%= form.text_field :typesOfEntrees, id: :entree_typesOfEntrees %>
  </div>

  <div class="field">
    <%= form.label :prices %>
    <%= form.text_field :prices, id: :entree_prices %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
